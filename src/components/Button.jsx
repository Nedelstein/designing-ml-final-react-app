import React from "react";

const buttonStyle = {
  margin: "10px 10px 10px 0",
  background: "black",
  color: "white"
};

let Button = () => {
  return <button style={buttonStyle}>Close</button>;
};

export default Button;
